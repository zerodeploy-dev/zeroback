// @zeroback/runtime is deprecated — use @zeroback/server/runtime instead.
// This package re-exports for backwards compatibility.
export { createZerobackDO, workerHandler } from "@zeroback/server/runtime"
export type { RuntimeConfig, FunctionDef, Env } from "@zeroback/server/runtime"
