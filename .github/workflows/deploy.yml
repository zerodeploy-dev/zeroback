# Auto-generated by ZeroDeploy
# Docs: https://zerodeploy.dev/docs/github#customizing
name: Deploy zeroback to ZeroDeploy

on:
  push:
    branches: [main]
    paths:
      - "docs/**"
  pull_request:
    types: [opened, synchronize, reopened]
    paths:
      - "docs/**"

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: oven-sh/setup-bun@v2

      - name: Install dependencies
        run: bun install

      - name: Build
        run: bun run build
        working-directory: docs

      - name: Deploy to ZeroDeploy
        run: npx @zerodeploy/cli deploy zeroback --dir docs/dist
        env:
          ZERODEPLOY_TOKEN: ${{ secrets.ZERODEPLOY_TOKEN }}
